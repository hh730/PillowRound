import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  sortContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingLeft: 16,
  },
  sortTextStyle: {color: 'white', fontSize: 20},
});

export default styles;
